# Staging IngressRoute for Traefik.
# Accessible at staging-api.lurus.cn (sibling of the r6-stage
# test-newhub.lurus.cn host — both terminate at the same Service).
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: lurus-newhub-staging
  namespace: lurus-newhub
  labels:
    app: lurus-newhub
    environment: staging
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`staging-api.lurus.cn`)
      kind: Rule
      services:
        - name: lurus-newhub
          port: 8850
  tls:
    secretName: lurus-cn-wildcard-tls
---
# TLS Certificate via cert-manager
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: staging-api-lurus-cn-tls
  namespace: lurus-newhub
spec:
  secretName: staging-api-lurus-cn-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - staging-api.lurus.cn
---
# HTTP to HTTPS redirect
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: lurus-newhub-staging-http
  namespace: lurus-newhub
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`staging-api.lurus.cn`)
      kind: Rule
      middlewares:
        - name: redirect-https
          namespace: traefik
      services:
        - name: lurus-newhub
          port: 8850
