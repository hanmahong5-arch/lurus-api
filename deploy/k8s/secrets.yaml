# Lurus-API Secrets
# This file should NOT be committed to git with real values
# Use placeholder values here and update in cluster
apiVersion: v1
kind: Secret
metadata:
  name: lurus-api-secrets
  namespace: lurus-system
type: Opaque
stringData:
  # Database connection
  SQL_DSN: "postgres://lurus:LurusOps2026@lurus-pg-rw.database.svc.cluster.local:5432/lurus?sslmode=disable"

  # Session secret (at least 32 chars)
  SESSION_SECRET: "LurusApiSessionSecret2026Secure!"

  # Zitadel OIDC (创建客户端后填入实际值)
  # 1. 访问 https://auth.lurus.cn 管理控制台
  # 2. 创建 Project -> 创建 Application (Web, PKCE)
  # 3. 设置 Redirect URI: https://api.lurus.cn/oauth/oidc
  ZITADEL_CLIENT_ID: "TODO_CONFIGURE_IN_ZITADEL_CONSOLE"
  ZITADEL_CLIENT_SECRET: "TODO_CONFIGURE_IN_ZITADEL_CONSOLE"
