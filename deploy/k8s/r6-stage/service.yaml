apiVersion: v1
kind: Service
metadata:
  name: lurus-newhub
  namespace: lurus-staging
  labels:
    app: lurus-newhub
spec:
  type: NodePort
  ports:
    - port: 8850
      targetPort: 3000
      nodePort: 30850
      name: http
  selector:
    app: lurus-newhub
