# Story 9-1 — Tier-3 Modality Usage Audit (2026-05-20)

**SQL**: `scripts/e9-1-prod-usage.sql` · **Hosts**: R1 PROD (`100.98.57.55` → `lurus-pg-1`, db=`newapi`, user=`postgres`); R6 STAGE (`100.122.83.20` → `lurus-pg-0`, db=`newhub`, user=`lurus`). Wrapper `scripts/story-9-1-tier3-audit-drill.sh` bypassed (default-db bug, see below); ran inline `kubectl exec psql`. Exit 0 both.

## Headline

**Tier-3 modalities (MJ / Suno / Music / Video / Realtime) = ZERO usage on PROD and STAGE over last 30 days. E9 deprecation can proceed without customer impact** — unblocks ~7,082 LOC removal (per `_bmad-output/planning-artifacts/story-9-1-tier3-usage-audit.md`).

## Results (last 30d)

- Q1 requests+quota per tier-3 model: 0 rows both envs.
- Q2 per category (MJ/Suno/Music/Video/Realtime): 0 requests, null quota, both envs.
- Q3 active tier-3 channels (type 2/5/36/50/51/52/54/55, any status): 0 both envs.

## Sanity-check (honesty §1 — `0/0/0` reflex doubt)

`0` is real, not a SQL where-clause bug. PROD R1 (type=2 consume, 30d) = **19,291 logs**. Top model_name 100% pure LLM: deepseek-chat 14,220 · gemini-2.5-flash 2,292 · deepseek-v4-flash 1,219 · deepseek-v3-2-251201 704 · llama-4-scout 392 · deepseek-v4-pro 264 · (tail: llama/gemini-3.x/doubao/nano-banana). No `mj-*`/`suno-*`/`sora-*`/`kling-*`/`jimeng-*`/`*video*`/`*realtime*` patterns. PROD channel types: only 7 distinct (1,17,20,24,26,43,45), all single-instance LLM types, none tier-3.

## Decision support

Customer impact 0, revenue impact 0, channel inventory 0. **Recommend proceeding to E9-2 (deprecation announce, 90-day window per D3) → E9-3 (code removal post-window)** without further usage data.

## Caveats / known issues

- Only 30-day window (60+-day re-entrants covered by 90-day deprecation window).
- Does NOT check cross-service tier-3 imports (lucrum/forge/etc) — grep is a follow-up before E9-3.
- All metrics are server-side `logs` reads, not external-client perspective.
- **`scripts/story-9-1-tier3-audit-drill.sh` default-db bug**: hardcoded `\${POSTGRES_DB:-lurus_hub}` reads remote shell env not local; needs `--db <name>` flag (R6=`newhub`, R1=`newapi`). Bypassed this run.
- Raw outputs: `e9-1-tier3-usage-20260520-raw.txt` (STAGE) + `-prod-raw.txt` (PROD).
